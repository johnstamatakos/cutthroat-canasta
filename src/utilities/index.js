export default class Accounts {
    constructor() {}

    async create({ username, password } = {}) {
        return username;
    }

    async logIn({ username, password } = {}) {
        return username;
    }
}
